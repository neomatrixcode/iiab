name: GitHub Action

on: [push]

jobs:
  execute-command:
    uses: neomatrixcode/vpniiab/.github/workflows/vpniiab.yml@master
    with:
      host: 10.0.2.15
      port: 53
      username: iiab-admin
      password: g0adm1nHT
      script: |
        ls
        bash multipass launch 22.04 --bridged -n u2310 -c 2 -d 25G -m 2G --cloud-init iiab-pr3593-none.yml && multipass shell u2310
    
